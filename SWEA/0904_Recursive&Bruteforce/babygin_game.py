# 연속 숫자 3개 이상 => run
# 같은 숫자 3개 이상 => triplet
# 무승부 => 0 출력

T = int(input())

# 플레이어 1, 플레이어 2 각각 점수 넣을 것
for test_case in range(1, T+1):
    cards = list(map(int, input().split()))
    
    # 카운팅 정렬 사용
    p1 = [0] * 10
    p2 = [0] * 10
    

def game(cards):
    for x in cards:
        
        
        


# 아이디어
# - 각 플레이어가 가진 숫자(0~9)의 개수를 카운팅 배열로 관리한다. ex) cntA[숫자] += 1
# - 카드를 한 장 받을 때마다 해당 플레이어가 즉시 승리(run 또는 triplet)했는지 검사하고,
#   승리하면 바로 종료(early stop). 끝까지 없으면 0(무승부).
#
# - triplet: 같은 숫자 3장 이상 -> cnt[x] >= 3
# - run: 연속된 숫자 3장 이상 -> (s, s+1, s+2) 각각 최소 1장씩.
#   "새로 받은 카드 x"가 반드시 승리에 기여해야 하므로
#   시작점 s는 [x-2, x-1, x] 세 가지만 보면 충분(범위 체크 필요: 0 <= s <= 7).
#   (매번 0~7 전체를 훑어도 되지만, 위 최적화가 더 깔끔)



